# 2026-07-22

## Sprint 5-4A initial flow

- Made urinal selection available from Spawn while retaining the single selection slot and ticket rules.
- Kept early reservation independent from movement: NPCs remain in registration-order FIFO and follow Queue, DecisionPoint, ApproachPoint, MovePoint, and UsePoint without a Spawn-to-urinal shortcut.
- Added a combo timing-start gate that opens on the first successful urinal-use transition and resets with the session.
- Added `Nyoice/Validate Sprint5-4A Initial Flow` for early selection, FIFO compaction and priority, ordered routing, timing start, reset, and required references.

## Setup material reuse

- Replaced transient embedded setup materials with reusable material assets selected by shader and color.
- Added migration of existing GameStage renderer materials to stable asset references so repeated Setup Game Stage runs do not create nested material instances.
- Kept Edit Mode setup code on `sharedMaterial` and preserved the configured display colors.
